# -*- coding: utf-8 -*-
import rules

tests = [
    "我攒了10片药准备今晚吃了，站在天台上看着下面",
    "最近捶墙捶到拳头流血，头撞墙撞破皮，真的撑不住了",
    "今晚可能就结束了，这是最后一次发帖，再见了大家",
    "我想划手、撕指甲、撞墙，只有这种方式能让我平静",
    "安全约定签了吗？紧急联络人是谁？替代自伤的舒缓方法有哪些？",
    "我好累，不想活了，但是又不敢死",
]

print("=== 规则模型测试（访谈高风险样本）===")
for t in tests:
    r = rules.analyze(t)
    print("\n文本: " + t[:50] + "...")
    print("  情绪极性: " + r["情绪极性"]["label"] + " (命中: " + str(r["情绪极性"]["matched"][:5]) + ")")
    print("  求助信号: " + r["求助信号"]["label"] + " (命中: " + str(r["求助信号"]["matched"][:5]) + ")")
    print("  安全风险: " + r["安全风险"]["label"] + " (命中: " + str(r["安全风险"]["matched"][:8]) + ")")
    print("  情感强度: " + str(r["情感强度"]["强度"]))